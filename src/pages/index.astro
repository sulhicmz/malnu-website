---
import Base from '../layouts/Base.astro';
import ExternalServices from '../components/ExternalServices.astro';
import NewsList from '../components/NewsList.astro';
import {
  heroImageAvif,
  heroImageWebp,
  heroImageWidth,
  heroImageHeight
} from '../data/heroImageBase64';
const pageTitle = 'Beranda | MA MALNU Kananga';
const description =
  'Portal resmi MA MALNU Kananga dengan layanan PPDB online, e-learning, arsip digital, jejaring alumni, berita pendidikan, dan bantuan beasiswa terbaru.';
---
<Base title={pageTitle} description={description}>
  <section class="hero card" aria-labelledby="hero-title">
    <div class="hero-body">
      <div class="hero-text">
        <p class="eyebrow">Madrasah Aliyah Malnu Kananga</p>
        <h1 id="hero-title">
          Mencetak generasi islami, unggul akademik dan berdaya saing global.
        </h1>
        <p>
          Selamat datang di portal digital MA MALNU Kananga. Temukan layanan terpadu untuk peserta didik, guru, orang tua,
          dan alumni dalam satu genggaman.
        </p>
        <div class="cta-group">
          <a class="button glow" href="/ppdb">Daftar PPDB</a>
          <a class="button secondary glow" href="/e-learning">Masuk E-Learning</a>
        </div>
      </div>
      <div class="hero-card" aria-label="Agenda kegiatan madrasah">
        <h2>Agenda Terkini</h2>
        <ul>
          <li><strong>25 Nov:</strong> Webinar Digitalisasi Madrasah</li>
          <li><strong>1 Des:</strong> Batas akhir pengisian rapor RDM</li>
          <li><strong>5 Des:</strong> Kegiatan Maulid Nabi bersama santri</li>
        </ul>
      </div>
    </div>
    <figure class="hero-visual">
      <picture>
        <source type="image/avif" srcset={`data:image/avif;base64,${heroImageAvif}`} />
        <source type="image/webp" srcset={`data:image/webp;base64,${heroImageWebp}`} />
        <img
          src={`data:image/webp;base64,${heroImageWebp}`}
          width={heroImageWidth}
          height={heroImageHeight}
          alt="Ilustrasi siswa belajar daring dengan perangkat digital"
          loading="eager"
          decoding="async"
          fetchpriority="high"
        />
      </picture>
    </figure>
  </section>

  <ExternalServices description="Pintasan menuju portal resmi Kementerian Agama dan Kemendikbud untuk layanan akademik." />

  <section class="grid highlights">
    <article class="card">
      <h2 class="section-title">PPDB Online 2025</h2>
      <p>
        Pendaftaran peserta didik baru kini serba daring. Siapkan berkas dan lengkapi formulir secara mudah dengan
        verifikasi otomatis.
      </p>
      <a class="button" href="/ppdb">Pelajari Alur</a>
    </article>
    <article class="card">
      <h2 class="section-title">E-Learning</h2>
      <p>
        Akses materi pembelajaran, jadwal kelas, dan pengumuman penting kapan pun dengan modul digital yang terhubung
        dengan guru pengampu.
      </p>
      <a class="button" href="/e-learning">Lihat Materi</a>
    </article>
    <article class="card">
      <h2 class="section-title">Jejaring Alumni</h2>
      <p>
        Terhubung dengan alumni lintas angkatan, bangun kolaborasi karier, dan bagikan kisah inspiratif melalui katalog
        alumni interaktif.
      </p>
      <a class="button" href="/alumni">Data Alumni</a>
    </article>
  </section>

  <section class="card info-section">
    <h2 class="section-title">Layanan Cepat</h2>
    <div class="grid grid-2 quick-service">
      <div>
        <h3>Rapor Digital Madrasah</h3>
        <p>Gunakan portal resmi RDM untuk melihat nilai dan progres belajar secara real-time.</p>
        <a class="button" href="https://rdm.ma-malnukananga.sch.id" target="_blank" rel="noopener">Masuk RDM</a>
      </div>
      <div>
        <h3>Absensi QR</h3>
        <p>Pindai QR di setiap kegiatan untuk pencatatan kehadiran otomatis dengan proteksi token.</p>
        <a class="button" href="/absensi">Panduan Absensi</a>
      </div>
    </div>
  </section>

  <NewsList title="Berita Pendidikan" endpoint="/api/news" emptyText="Belum ada berita dari cache." />
  <NewsList title="Informasi Beasiswa" endpoint="/api/scholarships" emptyText="Belum ada data beasiswa." />
</Base>
<style>
  .hero {
    position: relative;
    display: grid;
    gap: clamp(1.5rem, 4vw, 2.5rem);
    padding: clamp(1.5rem, 4vw, 2.75rem);
    background: radial-gradient(circle at top right, rgba(0, 166, 166, 0.25), transparent 60%), rgba(255, 255, 255, 0.92);
    overflow: hidden;
  }

  .hero::before,
  .hero::after {
    content: '';
    position: absolute;
    inset: -40%;
    background: radial-gradient(circle, rgba(0, 166, 166, 0.12) 0%, transparent 60%);
    animation: heroGlow 12s ease-in-out infinite;
    z-index: 0;
  }

  .hero::after {
    animation-delay: 6s;
    background: radial-gradient(circle, rgba(46, 125, 50, 0.18) 0%, transparent 60%);
  }

  .hero-body {
    display: grid;
    gap: clamp(1.25rem, 4vw, 2rem);
    position: relative;
    z-index: 1;
  }

  .hero-text {
    display: grid;
    gap: 1rem;
  }

  .eyebrow {
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-secondary);
    font-weight: 600;
  }

  h1 {
    font-size: clamp(2rem, 6vw, 3rem);
    margin-bottom: 0.25rem;
  }

  .cta-group {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .glow {
    position: relative;
    box-shadow: 0 12px 30px rgba(0, 166, 166, 0.25);
    overflow: hidden;
  }

  .glow::after {
    content: '';
    position: absolute;
    inset: -120%;
    background: conic-gradient(from 90deg, rgba(255, 255, 255, 0.18), rgba(255, 255, 255, 0));
    animation: shimmer 3.6s linear infinite;
    pointer-events: none;
  }

  .hero-card {
    border-radius: 1.5rem;
    background: rgba(46, 125, 50, 0.08);
    padding: 1.25rem;
    box-shadow: inset 0 0 0 1px rgba(46, 125, 50, 0.15);
    display: grid;
    gap: 0.75rem;
    position: relative;
    z-index: 1;
  }

  .hero-card ul {
    margin: 0;
    padding-left: 1.25rem;
    display: grid;
    gap: 0.5rem;
  }

  .hero-visual {
    margin: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 1.5rem;
    overflow: hidden;
    box-shadow: var(--shadow-glow);
    background: rgba(46, 125, 50, 0.12);
    position: relative;
    z-index: 1;
    animation: heroFloat 10s ease-in-out infinite;
    order: -1;
  }

  .hero-visual picture,
  .hero-visual img {
    display: block;
    width: 100%;
    height: auto;
  }

  .highlights {
    margin-top: 1.5rem;
  }

  .info-section {
    margin-top: 1.5rem;
  }

  .quick-service {
    margin-top: 1rem;
  }

  @media (min-width: 768px) {
    .hero {
      grid-template-columns: minmax(0, 1.05fr) minmax(0, 0.95fr);
      align-items: center;
    }

    .hero-visual {
      order: initial;
    }

    .hero-body {
      gap: 2rem;
    }

    .hero-card {
      max-width: 22rem;
    }

    .highlights {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @keyframes heroGlow {
    0%,
    100% {
      transform: translateY(-8%) rotate(6deg) scale(1.05);
      opacity: 0.85;
    }
    50% {
      transform: translateY(6%) rotate(-4deg) scale(1.15);
      opacity: 0.45;
    }
  }

  @keyframes heroFloat {
    0%,
    100% {
      transform: translateY(0px) scale(1);
    }
    50% {
      transform: translateY(-10px) scale(1.015);
    }
  }

  @keyframes shimmer {
    0% {
      transform: rotate(0deg);
      opacity: 0.25;
    }
    100% {
      transform: rotate(360deg);
      opacity: 0.7;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero::before,
    .hero::after,
    .hero-visual,
    .glow::after {
      animation: none;
    }
  }
</style>
