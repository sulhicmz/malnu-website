---
import ExternalServices from './ExternalServices.astro';
const year = new Date().getFullYear();
const menuLinks = [
  { href: '/', label: 'Beranda' },
  { href: '/profil', label: 'Profil' },
  { href: '/ppdb', label: 'PPDB' },
  { href: '/e-learning', label: 'E-Learning' },
  { href: '/alumni', label: 'Alumni' },
  { href: '/arsip', label: 'Arsip' },
  { href: '/berita', label: 'Berita' },
  { href: '/beasiswa', label: 'Beasiswa' },
  { href: '/kontak', label: 'Kontak' }
];
const socials = [
  { href: 'https://facebook.com/mamalnukananga', label: 'Facebook', handle: '@mamalnukananga' },
  { href: 'https://instagram.com/mamalnukananga', label: 'Instagram', handle: '@mamalnukananga' },
  { href: 'https://www.youtube.com/@mamalnukananga', label: 'YouTube', handle: 'MA MALNU Kananga' }
];
---
<footer class="site-footer" role="contentinfo">
  <div class="site-footer__inner">
    <div class="site-footer__brand">
      <h2>Madrasah Aliyah MA MALNU Kananga</h2>
      <p>Jl. Pendidikan No. 1 Kananga, Menes, Kabupaten Pandeglang, Banten 42262</p>
      <dl>
        <div>
          <dt>Telepon</dt>
          <dd><a href="tel:+62253234567">(0253) 234 567</a></dd>
        </div>
        <div>
          <dt>Email</dt>
          <dd><a href="mailto:info@ma-malnukananga.sch.id">info@ma-malnukananga.sch.id</a></dd>
        </div>
        <div>
          <dt>NPSN</dt>
          <dd>20600295</dd>
        </div>
        <div>
          <dt>NSM</dt>
          <dd>131227620001</dd>
        </div>
      </dl>
    </div>
    <div class="site-footer__columns">
      <div class="site-footer__column">
        <h3>Menu</h3>
        <ul>
          {menuLinks.map((link) => (
            <li>
              <a href={link.href}>{link.label}</a>
            </li>
          ))}
        </ul>
      </div>
      <div class="site-footer__column">
        <h3>Layanan Eksternal</h3>
        <ExternalServices variant="compact" heading="Layanan Eksternal" />
      </div>
      <div class="site-footer__column">
        <h3>Ikuti Kami</h3>
        <ul>
          {socials.map((item) => (
            <li>
              <a href={item.href} target="_blank" rel="noopener noreferrer">{item.label} <span>{item.handle}</span></a>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </div>
  <div class="site-footer__meta">
    <p>© {year} MA MALNU Kananga. Seluruh hak cipta dilindungi.</p>
    <nav aria-label="Kebijakan website">
      <a href="/kebijakan-privasi">Kebijakan Privasi</a>
      <a href="/syarat-penggunaan">Syarat Penggunaan</a>
    </nav>
  </div>
</footer>
<style>
  .site-footer {
    background: linear-gradient(160deg, #12461c, #0a2710);
    color: #f3ffe5;
    padding: clamp(3rem, 7vw, 4.5rem) clamp(1.5rem, 5vw, 4rem);
    margin-top: clamp(3rem, 10vw, 6rem);
  }

  .site-footer__inner {
    display: grid;
    gap: clamp(2rem, 6vw, 3.5rem);
    max-width: 1100px;
    margin: 0 auto;
  }

  .site-footer__brand h2 {
    margin: 0 0 0.75rem;
    font-size: clamp(1.3rem, 3vw, 1.75rem);
  }

  .site-footer__brand p {
    margin: 0;
    max-width: 46ch;
    line-height: 1.6;
  }

  .site-footer__brand dl {
    margin: 1.25rem 0 0;
    display: grid;
    gap: 0.75rem;
  }

  .site-footer__brand dt {
    font-weight: 600;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(243, 255, 229, 0.7);
  }

  .site-footer__brand dd {
    margin: 0;
    font-size: 0.95rem;
  }

  .site-footer__columns {
    display: grid;
    gap: clamp(2rem, 6vw, 3rem);
  }

  .site-footer__column h3 {
    margin-bottom: 1rem;
    font-size: 1.05rem;
    color: #e9ffe0;
  }

  .site-footer__column ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: 0.65rem;
  }

  .site-footer__column a {
    color: rgba(243, 255, 229, 0.85);
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
  }

  .site-footer__column a:hover,
  .site-footer__column a:focus-visible {
    color: #ffffff;
  }

  .site-footer__meta {
    border-top: 1px solid rgba(243, 255, 229, 0.16);
    margin-top: clamp(2.5rem, 6vw, 3.5rem);
    padding-top: clamp(1.5rem, 4vw, 2rem);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: flex-start;
  }

  .site-footer__meta nav {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .site-footer__meta a {
    color: rgba(243, 255, 229, 0.8);
  }

  .site-footer__meta a:hover,
  .site-footer__meta a:focus-visible {
    color: #ffffff;
  }

  @media (min-width: 768px) {
    .site-footer__inner {
      grid-template-columns: minmax(0, 1.3fr) minmax(0, 1fr);
      align-items: start;
    }

    .site-footer__columns {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .site-footer__meta {
      flex-direction: row;
      justify-content: space-between;
      align-items: center;
    }
  }
</style>
